extends: l9_central.yaml
name: l9_central_local
description: >-
  l9_central run against a local open-weight decision model (JEV_PROVIDER=local): the local
  server answers one request at a time, so few requests in flight and a long timeout.
jev:
  max_concurrency: 2
  timeout_s: 300
